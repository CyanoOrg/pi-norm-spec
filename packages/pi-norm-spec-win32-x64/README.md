# pi-norm-spec-win32-x64

Native Windows x64 runtime for the exact same-version `pi-norm-spec` root
package. It carries the Rust bridge and one sealed norm-spec release payload.
It is installed through the root package and is not a standalone semantic
implementation.
